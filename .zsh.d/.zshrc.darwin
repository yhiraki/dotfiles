alias ls="ls -G"
alias ll="ls -lG"
alias la="ls -laG"

function nas () {
  echo -E $1 \
  | sed 's/^ *//g' \
  | sed 's/ *$//g' \
  | tr '\' '/' \
  | tr -d '\n' \
  | sed 's/^/smb:/g' \
  | xargs -E -t open
}
