alias ls="ls -G"
alias ll="ls -lG"
alias la="ls -laG"

function nas () {
  echo -E $1 \
  | tr -d '\n' \
  | sed 's/^ *//g' \
  | sed 's/ *$//g' \
  | tr '\' '/' \
  | sed 's/^/smb:/g' \
  | xargs -I{} -t open {}
}
